■ 파일 업로드- multer 모듈

 ο npm 설치
    - Express에서는 body-parser 모듈과 함께 multer 모듈을 사용해서 파일 업로드를 구현할 수 있다.
    - body-parser 모듈은 express-generator로 설치하면 이미 미들웨어로 등록되어 있으므로, multer 모듈만 추가적으로 설치하면 된다.

   c:\work\myapp>npm install --save multer

